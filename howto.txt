dotnet publish -c Release -o ./publish
cd publish
zip -r ../app.zip .
cd ..
az webapp deployment source config-zip --resource-group bookslibrary_group --name bookslibrary --src app.zip

#to run with debugging (works with vscode)
dotnet watch --project ordersSystem.Web run

az webapp config set --resource-group bookslibrary_group  --name bookslibrary --startup-file "dotnet blazorBookLibrary.dll"